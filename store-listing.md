## 爱问答 · 网课学习助手

一键解析当前页面的试题并获取答案，支持作业 / 考试 / 章节测验的自动收录与答题，答完可自动提交（考试卷除外）；视频、文档等课程学习任务自动推进。

- 官网：<https://www.aiask.site/>
- 遇到问题：<https://www.aiask.site/contact.html>

### 已适配平台

| 平台 | 支持范围 |
| --- | --- |
| 超星学习通 | 章节测验、作业等；课程学习（视频 / 音频 / 文档 / 阅读任务点） |
| 168 网校 | 章节测评 |

更多平台持续适配中。表外的平台不会注入，装了也不会出现面板。

### 怎么开始

1. 先装一个脚本管理器：Tampermonkey 或脚本猫任选其一
2. **给脚本管理器打开「允许用户脚本」开关**，见下一节。Chrome 默认是关的，不开则脚本不会运行
3. 安装本脚本
4. 打开课程页 / 作业页 / 章节测验页，右下角会出现面板

首次使用会在后台同步一次云端识别规则，同步完成后自动重新识别，不用手动点。

### 装完没反应？先检查这个开关

Chrome 把「运行用户脚本」做成了每个扩展一个独立开关，**默认关闭**。它没打开时脚本一行都不会执行，而页面上不会有任何提示：没有面板、没有报错，看起来和「这个站点还没适配」一模一样。

**Tampermonkey**

1. 地址栏打开 `chrome://extensions/`，找到 Tampermonkey，点「详情」
2. 打开那个叫「允许用户脚本」（有的版本写「允许运行用户脚本」）的开关
3. 回到课程页刷新一次

**脚本猫**

1. 同样进 `chrome://extensions/` 里脚本猫的「详情」，打开「允许用户脚本」
2. **再把脚本猫这个扩展停用一次，然后重新启用**。脚本猫比 Tampermonkey 多这一步，不做的话开关不生效
3. 回到课程页刷新一次

如果你的浏览器里找不到这个开关，说明它不需要，跳过这一节即可。

### 关于答案

- 答案与页面上的选项对不上时**不写也不点**，宁可留空也不给错答案
- 多选、匹配、多空这类题整题要么全填要么不填，不留半成品
- 考试卷任何设置下都不会替你交
- 平台改版由云端规则修复，通常不需要等脚本更新
- 答案不保证正确，重要的卷子请自己核对一遍

### 常见问题

**装完打开页面没反应？**
按顺序查三样：① 先看「装完没反应？先检查这个开关」那一节，这是最常见的原因；② 确认当前页在上面的适配范围里；③ 都对的话，到面板「系统 · 通用」的「规则更新」点一下「检查更新」。

**面板挡住页面了？**
按住面板顶部那一条可以拖到别处；右上角的「−」把它收起来，需要时再点开。位置会记住。

**为什么有的题空着？**
没查到答案，或者答案与页面选项无法可靠对应——这两种情况都宁可留空，不会瞎填。

**能不能不自动提交？**
能。「系统 · 通用 · 答题行为」里有「整卷答完自动提交」的开关；旁边的「提交阈值」决定整卷要有多大比例拿到可信答案才提交，不达标就只暂存。考试卷任何设置下都不会替你交。

**正在自动播视频，想停下来？**
在课程章节页时，面板首页的「课程学习」那块有暂停，暂停会同时停下正在播放的视频。想彻底关掉，或者调播放倍速、决定只处理哪几类任务点，去「系统 · 课程」。

**做过的题下次还要重新查吗？**
不用。做过并出分的题目会收录到本机，再遇到同一道直接命中，不联网。当前这一页的收录在「收录」页看，全部记录在「系统 · 缓存」。

**换电脑或者重装浏览器，收录会丢吗？**
会。到「系统 · 缓存」导出成文件存好，新机器上用「文件导入」装回来。

**平台改版了是不是就用不了？**
识别规则从云端更新，每 24 小时自动检查，多数改版不用等脚本更新。想立刻试一次，到「系统 · 通用 · 规则更新」点「检查更新」。

**怎么看当前是哪个版本？**
「系统 · 诊断」里有脚本版本和各规则包的版本。反馈问题时把这一页的信息带上，最省事。

### 免责声明

本脚本仅供学习与交流使用，答案不保证正确，请自行判断并承担使用后果。
